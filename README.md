
  # Convert HTML to React Vite

  This is a code bundle for Convert HTML to React Vite. The original project is available at https://www.figma.com/design/cC9aPEo509KJxQrU2dWBC3/Convert-HTML-to-React-Vite.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  